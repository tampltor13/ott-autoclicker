OTT AutoClicker — Changelog
===========================


v2.0.14 — Peacock: freeze_live_selector + wait nakon profile clicka + click_delay 3s
-----------------------------------------------------------------------
- Dodano freeze_live_selector za Peacock: //*[@data-testid="watch-button"]
  Nakon što se klikne Ingrid profil, watch-button se klikne automatski
- Dodano time.sleep(3) u _freeze_try_profile_select() nakon uspješnog clicka
  da se profil stigne učitati prije nego _try_live_selector() pokuša naći watch-button
- click_delay povećan s 2 na 3 sekunde (razmak između klikova na targete)

v2.0.13 — Peacock: ispravak freeze_profile_selector (klik na Ingrid profil)
-----------------------------------------------------------------------
- Stari XPath klikao je container div (profiles__avatars--slider) koji nije selectable
- Novi XPath cilja točni clickable element: profiles__avatar--image s aria-label="Ingrid"
  //div[contains(@class,"profiles__avatar--image") and contains(@aria-label,"Ingrid")]
- Ovaj element ima role="button" i tabindex="0" — pravi clickable

v2.0.12 — Pre-check: page health check (404 / sorry detekcija)
-----------------------------------------------------------------------
- Pre-check sada nakon IP provjere refresha event stranicu i skenira sadržaj
- Traži poznate error fraze: "sorry", "couldn't find", "page not found", "404"
- Ako se detektira greška → šalje SMTP mail "🔴 Page Error Detected [Platform]"
  - Naranačsti header (distinktivan od crvenog "Browser Disconnected" maila)
  - Prikazuje: platform, detektirana fraza, URL koji je failao, check time, start time
- Koristi isti smtp_config.txt — nije potrebna nikakva izmjena konfiguracije
- Primjer: night pre-check u 23:00 bi otkrio Amazon 404 i dao vremena za ispravak URL-a
- Nova metoda: _precheck_page_health(), _send_page_health_alert(), _build_page_health_alert_html()

v2.0.11 — Nova platforma: Viaplay DK
-----------------------------------------------------------------------
- Dodana platforma Viaplay DK (https://viaplay.dk/)
- Button: //*[@data-testid="play-button-text"]
- refresh_first: True, load_wait: 10s, freeze_recovery: refresh_only (video nastavlja sam)
- Scan offset: 30 min; freeze detection ON po defaultu
- Default browser: Chrome
- Scan parser: "I dag kl. 07.30" format (danski, točka kao separator)
  Podržava: I dag (danas), I morgen (sutra), danski dani u tjednu (Mandag, Tirsdag…)
  JS čita [class*="StartTime_container"] element; fallback na cijeli page_text
- Channels.csv: Denmark, Viaplay DK, C22OTT, Chrome

v2.0.10 — U-Next: ispravak (maknut video_detect, samo klik)
-----------------------------------------------------------------------
- U-Next nije video_detect platforma — autoclicker jednostavno klikne play button
- Button: //*[@data-testid="liveTitleDetail-stage-play"] ("ライブ配信を視聴")
- refresh_first: True, load_wait: 15s, scan_offset: 30 min
- Korisnik navigira na konkretan live event URL; PLATFORMS homepage je samo polazišna točka

v2.0.9 — Nova platforma: U-Next
-----------------------------------------------------------------------
- Dodana platforma U-Next (https://video.unext.jp/)
- Button: //*[@data-testid="liveTitleDetail-stage-play"] ("ライブ配信を視聴")
- refresh_first: True, load_wait: 15s, video_detect: True, freeze_recovery: refresh_only
- Scan offset: 30 min
- Default browser: Edge
- Channels.csv: Japan, U-Next, C314 Japan, Edge

v2.0.8 — Uklonjen windowed fullscreen (pre bugovito)
-----------------------------------------------------------------------
- Maknuta metoda _inject_windowed_fullscreen() i poziv iz driver startup-a
- Fullscreen se ponaša standardno — browser/OS fullscreen kao i prije

v2.0.7 — Windowed fullscreen: auto-restore nakon freeze refresha
-----------------------------------------------------------------------
- Problem: page refresh (freeze detection) brisao je fake fullscreen jer DOM element nestane
- Rješenje: localStorage persistencija — pri ulasku u fake fullscreen, sprema se ključ
  tipa "IFRAME:www.dailymotion.com" ili "VIDEO"; pri ESC izlasku ključ se briše
- Na svakom page loadu (addScriptToEvaluateOnNewDocument): auto-restore logika čita
  localStorage, traži isti tip elementa i re-enters fake fullscreen
- Za iframeove: traži po hostname (npr. iframe[src*="www.dailymotion.com"])
- Retry petlja: pokušava do 20 puta × 500ms (~10s) dok element nije dostupan u DOM-u
- ESC i dalje briše localStorage pa sljedeći refresh NE restore-a (korisnik je izišao)

v2.0.6 — Windowed fullscreen: ispravak za cross-origin iframeove (Dailymotion)
-----------------------------------------------------------------------
- v2.0.5 nije radio za Teleantillas/Dailymotion jer Page.addScriptToEvaluateOnNewDocument
  ne injektira u cross-origin iframeove (geo.dailymotion.com je poseban renderer proces)
- Novo: fullscreenchange listener u main frameu hvata NATIVNI OS fullscreen event
  koji bublira iz child iframea, odmah poziva PRAVI document.exitFullscreen() i
  CSS-ekspandira iframe element na 100vw × 100vh — bez OS fullscreena
- Nativni getter (Document.prototype.fullscreenElement) sprema se PRIJE overridea
  da bismo mogli razlikovati pravi OS fullscreen od sintetičkih events
- Isti ESC tipka za izlaz iz fake fullscreena

v2.0.5 — Windowed fullscreen: fullscreen ostaje unutar browser prozora
-----------------------------------------------------------------------
- Nova metoda _inject_windowed_fullscreen(): poziva CDP Page.addScriptToEvaluateOnNewDocument
  odmah nakon što se driver kreira — vrijedi za SVE stranice i SVE frameove (uključujući
  cross-origin iframeove kao geo.dailymotion.com)
- Klik na fullscreen gumb unutar video playera: video se proširi na 100vw × 100vh unutar
  browser prozora — ne odlazi na OS fullscreen koji bi preuzeo cijeli monitor
- ESC vraća video na originalnu veličinu
- fullscreenElement / webkitFullscreenElement su overrideani pa player UI ispravno
  prikazuje "exit fullscreen" kontrole
- Vrijedi za sve platforme automatski — nema potrebe za posebnim postavkama po platformi

v2.0.4 — Teleantillas: automatski unmute nakon freeze refresha
-----------------------------------------------------------------------
- Novi PLATFORM_RULES key: freeze_unmute_selector (XPath gumba za unmute)
- Nova metoda _try_unmute(): nakon refresha switchá u iframe(ove), traži gumb i klika ga
  Koristi isti freeze_iframe_src za navigaciju kroz iframeove (dailymotion.com → geo.dailymotion.com)
- Teleantillas: freeze_unmute_selector = '//button[@data-testid="tap-to-unmute"]'
  Video se nakon refresha pojavi mutiran — ovaj korak ga automatski odmutira
- switch_to.default_content() uvijek u finally bloku — sigurno izlaženje iz iframea
- Ostale platforme bez freeze_unmute_selector: ponašanje identično kao prije

v2.0.3 — Freeze iframe: podrška za ugniježđene iframeove + Teleantillas platforma
-----------------------------------------------------------------------
- freeze_iframe_src sada prihvaća i string i listu stringova
  Primjer: ["dailymotion.com", "geo.dailymotion.com"] — Selenium switchá razinu po razinu
- Dodana platforma Teleantillas (https://teleantillas.com.do/en-vivo/)
  freeze_iframe_src: ["dailymotion.com", "geo.dailymotion.com"]
  Default browser: Edge

v2.0.2 — Nova platforma: Dailymotion
-----------------------------------------------------------------------
- Dodana platforma Dailymotion (https://www.dailymotion.com)
- Default browser: Edge
- Bez video_detect — korisnik ručno navigira na live stream, pokreće Freeze Detection
- Freeze recovery: refresh_only
- Napomena: freeze_iframe_src nije potreban — na dailymotion.com video je u glavnom frameu

v2.0.1 — Freeze Detection: podrška za cross-origin iframe video (Dailymotion i sl.)
-----------------------------------------------------------------------
- Novi PLATFORM_RULES key: freeze_iframe_src (string s dijelom iframe src URL-a)
  Primjer: "freeze_iframe_src": "dailymotion.com"
- Kad je postavljen: Selenium switchá na iframe, uzima currentTime, switchá nazad
- Ako iframe nije pronađen: tiho pada na normalni JS u glavnom frameu
- switch_to.default_content() uvijek se poziva u finally bloku — nema zaostajanja u iframeu
- Postojeće platforme bez freeze_iframe_src: ponašanje identično kao prije

v2.0.0 — Major release: Nova platforma Victory+
-----------------------------------------------------------------------
- Dodana platforma Victory+ (https://victoryplus.com/)
- Mod: video_detect + refresh_first, load_wait 15s
- Default browser: Edge
- VPN precheck: US
- channels.csv: USA / Victory+ / C36 / Edge
- ESPN+ US channel ID-evi dodani: 372 (C17OTT), 3204 (C263 backup)

v1.0.100 — Freeze Detection: default trajanje produženo na 6 sati
-----------------------------------------------------------------------
- Default end time za Freeze Detection: start_time + 6h (bilo je +4h)
- Vrijedi kada monitoring završi i automatski pokrene freeze detection

v1.0.99 — ESPN+ US: ispravka video_detect za višestruke video elemente
-----------------------------------------------------------------------
- ESPN+ stranica ima 2 video elementa: [0] dummy (currentTime=0, paused, src="")
  i [1] pravi video (currentTime>0, playing, src=blob://)
- document.querySelector('video') uvijek vraća prvi (dummy) → check uvijek fail
- Fix: querySelectorAll + Array.some() — traži bilo koji video s currentTime > 1

v1.0.98 — ESPN+ US: tipka M nakon detekcije videa (unmute)
-----------------------------------------------------------------------
- Dodano video_detect_key: "m" za ESPN+ US
  ESPN+ startuje mjutan — nakon što video_detect potvrdi da video teče,
  šalje se tipka M za unmute (identično kao kod drugih platformi)

v1.0.97 — ESPN+ US Scan: uklonjen ET→CET/CEST offset (+6h)
-----------------------------------------------------------------------
- ESPN+ prikazuje vrijeme u lokalnoj vremenskoj zoni PCa (browser locale),
  ne u ET — "7:30 PM" je već lokalno (CEST) pa konverzija nije potrebna
- Maknuto: dt_et + timedelta(hours=6)
- Sada: parsed datetime se koristi direktno bez ikakve konverzije

v1.0.96 — Freeze: detekcija browser crash/OOM stranice
-----------------------------------------------------------------------
- Freeze loop sada eksplicitno prepoznaje crash URL-ove (chrome-error://, edge://crashedtab, about:neterror)
  u dvije točke: prije JS uzorkovanja i nakon JS iznimke
- Kad se detektira crash stranica: navigira direktno na platform URL umjesto refresh-a
  (refresh na OOM stranici može opet crashirati — navigate je sigurniji)
- Jasna log poruka: "Browser tab crashed (Out of Memory?) — navigating back to platform URL…"

v1.0.95 — Freeze tab: "Now" gumb nije ažurirao datum
-----------------------------------------------------------------------
- Bug: "Now" gumb u Freeze tabu postavljao je samo vrijeme, datum ostajao nepromijenjen
  Uočeno na PC-u u drugoj vremenskoj zoni (Japan) gdje je datum već bio +1 dan
- Fix: dodana _set_freeze_start_now() metoda koja postavlja i datum i vrijeme
  (identično kao _set_start_now() u Monitor tabu)

v1.0.94 — ESPN+ US: ispravka video_detect JS provjere
-----------------------------------------------------------------------
- video_detect_js za ESPN+ US: uklonjen !v.paused i v.readyState >= 3
  ESPN+ player prikazuje video ali Selenium ga vidi kao "paused" →
  Monitor tab vrtio petlju beskonačno iako je video bio aktivan
- Nova provjera: samo v.currentTime > 1 (identično freeze detection logici)
- Freeze Detection radio ispravno jer koristi isti currentTime pristup

v1.0.93 — Nova platforma: ESPN+ US + ET→CET/CEST scan konverzija
-----------------------------------------------------------------------
- Dodana platforma ESPN+ US (https://www.espn.com/watch/schedule)
- Mod: video_detect + refresh_first, load_wait 15s (identično DAZN)
- Default browser: Chrome, freeze detection: ON
- VPN precheck: US (provjera da je browser IP u SAD)
- Scan: _parse_espn() čita WatchPaywall__subtitle element
  Format: "Monday, July 6 | 4:00 PM" (US Eastern Time)
  Automatska konverzija ET → CET/CEST (+6h, vrijedi i ljeti i zimi)

v1.0.92 — WOWOW: "ライブを再生" dialog handling + generički freeze_live_selector
-----------------------------------------------------------------------
- Novi mehanizam freeze_live_selector: generički XPath klik nakon refresha
  Poziva se i u video_detect loopu (prvo učitavanje) i u freeze recovery loopu
  Silent no-op ako element nije prisutan — neće smetati ostalim platformama
- WOWOW: freeze_live_selector = '//a[contains(@class,"btn-fill") and contains(.,"ライブを再生")]'
  Klikne "ライブを再生" (Play Live) gumb u resume dijalogu ako se pojavi

v1.0.91 — Disney+ preimenovan u Disney+ US + channels.csv
-----------------------------------------------------------------------
- Platforma "Disney+" preimenovana u "Disney+ US" (PLATFORMS + PLATFORM_RULES)
- channels.csv: dodan redak USA / Disney+ US / C30OTT / Chrome

v1.0.90 — Nova platforma: WOWOW
-----------------------------------------------------------------------
- Dodana platforma WOWOW (https://wod.wowow.co.jp/live-schedule)
- Mod: video_detect (refresh_first) — identično kao DAZN, video kreće automatski nakon refresha
- Default browser: Edge
- Default freeze detection: ON, freeze_recovery: refresh_only
- Default scan_offset: 30 min (video dostupan 30 min prije live eventa)

v1.0.89 — Stan: Scan podrška + default offset 30 min
-----------------------------------------------------------------------
- Scan: dodan _parse_stan() parser za Stan format "3:00pm 29 June 2026"
  Čita direktno iz section.program__details-extended .program__extra via JS
  Fallback na cijeli page_text ako JS element nije dostupan
  Stan scan ne pada na generalne YYYY/MM/DD ili HH:MM fallback parsere
- Stan default scan_offset: 30 min (kao Prime Video)

v1.0.88 — Scan: Prime Video fix — puna imena mjeseci + WebDriverWait
-----------------------------------------------------------------------
- Amazon regex prihvaća puna imena mjeseci: June, July, August... (ne samo Jun, Jul, Aug)
- WebDriverWait(10s) čeka da se buy-box-msg element renderira prije čitanja
  (querySelector bez waita vraćao null jer element još nije bio u DOM-u)
- Za Prime Video nikad ne pada na YYYY/MM/DD regex (page ima misleading timestampove)
- Debug log ispisuje što je pročitano iz buy-box-msg

v1.0.87 — Scan: Amazon format samo za Prime Video + Setup tab scroll + UI fixes
-----------------------------------------------------------------------
- Scan: Amazon "Jun 28, 2026 3:00 AM CEST" format se primjenjuje SAMO za Prime Video platforme
  Za ostale platforme redosljed ostaje: YYYY/MM/DD → plain HH:MM
  Sprječava krivi match datuma na non-Prime stranicama
- Setup tab: scrollable canvas — Advanced mode sadržaj više nije odsječen
  Scroll wheel radi unutar Setup taba
- Combobox (Platform, Browser size): scroll wheel više ne mijenja vrijednost
- Scan gumb: padx poravnan s Now gumbom (4px umjesto 6px)

v1.0.86 — Scan offset: sve Prime Video platforme default 30 min
-----------------------------------------------------------------------
- Sve Prime Video platforme (USA/IT/BR/UK/DE/ES/JP/MX/FR) dobivaju scan_offset: 30
  Scan skida 30 min — npr. utakmica 00:30 → start monitoring 00:00 (ispravno, isti dan)
  Bez ovog, default 60 min bi dao 23:30 prethodne noći → night-check logika (23:00 prethodnog dana)

v1.0.85 — Scan: Amazon format + SMTP credentials iz lokalnog fajla
-----------------------------------------------------------------------
- Scan button: dodан Amazon format "Jun 26, 2026 12:30 AM CEST" s AM/PM konverzijom
  Redosljed pretrage: YYYY/MM/DD → Amazon MMM DD YYYY HH:MM AM/PM → plain HH:MM
- SMTP credentials premješteni iz koda u lokalni smtp_config.txt
  (KEY=VALUE format, nije u git repozitoriju)
- .gitignore dodan: smtp_config.txt, browser_profiles/, logs.txt

v1.0.84 — Pre-check: pametno vrijeme provjere + VPN country check + HTML mailovi
-----------------------------------------------------------------------
- Pre-check logika: 00:00–04:59 → check u 23:00 prethodne večeri
  05:00–23:59 → check 2 sata prije starta
- VPN country check: per-platform provjera IP-a iz browsera
  Sve Prime Video platforme imaju definiran expected country (US/IT/BR/GB/DE/ES/JP/MX/FR)
  Ako IP ne odgovara, šalje se zasebni "VPN Wrong Country" mail
- HTML mailovi: "Browser Disconnected" (crveni) i "VPN Wrong Country" (žuti)
  Sadrže: Platform, Check time, Monitoring start, Until live, Error/IP detalji
- SMTP konfiguracija: Amazon SES eu-central-1, from autoclicker@global-mmk.com
  to recording@global-mmk.com
- "Run precheck" gumb u Setup tabu za ručno pokretanje provjere
- Scan button: čita vrijeme s browser stranice, oduzima offset, puni Start date/time
- Scan time offset (min) spinbox u Setup tabu (advanced), per-platform via scan_offset rule
  NBA Docomo default: 10 min

v1.0.83 — Scan button: automatsko čitanje vremena s browser stranice
-----------------------------------------------------------------------
- Novi gumb "Scan" pored End time na Monitor tabu
- Čita tekst trenutno otvorene stranice u Selenium browseru
- Traži YYYY/MM/DD HH:MM format (npr. NBA Docomo countdown timer)
- Fallback: traži samo HH:MM ako nema punog datuma
- Oduzima konfigurirani offset (minuta) i puni Start date + Start time
- Novi spinbox "Scan time offset (min):" u Setup tabu (advanced mode)
  ispod "Scroll after click", default 60 min
- Per-platform scan offset u PLATFORM_RULES ("scan_offset" key)
  npr. NBA Docomo = 10 min
- Logira rezultat scana u Monitor log (teal boja)

v1.0.82 — Nova platforma: DAZN IT
-----------------------------------------------------------------------
- Dodana platforma "DAZN IT" (https://www.dazn.com/en-IT)
- Konfiguracija identična DAZN DE/DAZN ES (video_detect, refresh_first,
  load_wait 60s, freeze_recovery: refresh_only)
- AntiFreeze default ON (kao DAZN DE i DAZN ES)
- Default browser: Chrome (DAZN ES koristi Edge, DAZN IT ne)

v1.0.81 — DatePickerWidget: DD-MM picker sa strelicama
-----------------------------------------------------------------------
- Novi DatePickerWidget: prikazuje samo DD-MM (godina skrivena vizualno)
- Strelice gore/dolje mijenjaju dan ili mjesec s wraparound
- Auto-jump na MM nakon unosa 2 znaka dana
- Interno čuva puni YYYY-MM-DD format (kompatibilno s _parse_dt)
- Dan se automatski clampira prema broju dana u mjesecu
- Koristi se na sva 4 mjesta: Monitor start/end, Freeze start/end
- Maknut "+" gumb za datum (više nije potreban)

v1.0.80 — TimePickerWidget: HH:MM picker s klik-selekcijom
-----------------------------------------------------------------------
- Novi TimePickerWidget: dva Entry widgeta (HH i MM) s ":" između
- Klik na sate selektira samo sate, klik na minute samo minute
- Strelice gore/dolje mijenjaju vrijednost s wraparound (23→0, 59→0)
- Auto-jump na minute nakon unosa 2 znake sata
- Koristi se na sva 4 mjesta: Monitor start/end, Freeze start/end
- End time widgeti podržavaju praznu vrijednost (allow_empty=True)
- Potpuno kompatibilan s postojećim .get()/.set() pozivima

v1.0.79 — Pre-check: IP/VPN info iz browsera
-----------------------------------------------------------------------
- Nakon uspješnog browser pre-checka, otvara novi tab i dohvaća
  IP info s ipinfo.io/json — reflektira proxy/VPN ekstenziju u browseru
- Logira: IP adresu, grad, državu i ISP/VPN provider name
  npr. "Browser IP: 185.x.x.x | Amsterdam, NL | AS... NordVPN"
- Tab se automatski zatvara nakon provjere, vraća se na originalni tab
- Ako dohvat ne uspije, logira upozorenje (ne blokira ostalo)

v1.0.78 — Pre-check: provjera browser konekcije 2h prije starta
-----------------------------------------------------------------------
- Novi mehanizam: 2 sata prije zadanog start timea, provjeri je li
  Selenium driver još uvijek spojen (driver.execute_script("return 1"))
- Ako provjera uspije: logiraj "Pre-check OK" u log
- Ako provjera ne uspije: logiraj ERROR i pošalji mail notifikaciju
  sa subject: "🔴 AC - Browser Disconnected [Platforma]"
- SMTP konfiguracija u kodu: PRECHECK_SMTP_USER/PASS/HOST/PORT/MAIL_TO
  (prazno po defaultu — ako nije konfigurirano, samo logira upozorenje)
- Ako je start time < 2h od pokretanja monitoringa, pre-check se preskače
- Pre-check thread se automatski gasi ako se monitoring zaustavi

v1.0.77 — Dodana platforma Hotstar
-----------------------------------------------------------------------
- Hotstar: https://www.hotstar.com/in/home
- Default browser: Edge, default veličina: MD — 650×550
- Bez selektora za sada (platforma dodana radi pristupa)

v1.0.76 — DStv: profile-chooser recovery korak za freeze
-----------------------------------------------------------------------
- Nakon freeze refresha, DStv se nekad vrati na profile chooser
- Novi mehanizam freeze_profile_selector: provjeri i klikni avatar
  profila ako je prisutan, prije nastavka monitoringa
- DStv: freeze_profile_selector = //img[contains(@class,"Avatar_avatar__jkjO-")]
- Ako avatar nije prisutan, nastavlja normalno (video se sam nastavio)

v1.0.75 — Coupang Play: fix selektor za "Watch Live Now"
-----------------------------------------------------------------------
- Selektor //*[@data-cy="playCtaButtonText"] je matchao i "Upcoming"
  i "Watch Live Now" gumbe (isti data-cy atribut)
- Dodan uvjet contains(.,"Watch Live Now") — sada klika samo gumb
  koji je live, ne i nadolazeće programe

v1.0.74 — Stan: freeze detection via Shadow DOM
-----------------------------------------------------------------------
- Stan: video je unutar Shadow DOM (<stan-player> custom element)
- Novi mehanizam freeze_video_js: custom JS za čitanje currentTime po platformi
  umjesto globalnog querySelectorAll('video')
- Stan: freeze_video_js koristi stan-player.shadowRoot.querySelector('video')
  — ispravno čita currentTime=2073s

v1.0.73 — Stan: dispatch_click za pokretanje videa
-----------------------------------------------------------------------
- Novi mehanizam dispatch_click: koristi dispatchEvent(MouseEvent) umjesto
  arguments[0].click() — simulira pravi user gesture što Stan zahtijeva
  za autoplay (browser autoplay policy blokira obični JS click)
- Stan: dispatch_click enabled (uz force_js_click)

v1.0.72 — Log: datum u timestamp + auto-save u fajl
-----------------------------------------------------------------------
- Monitor i Freeze logovi: timestamp promijenjen iz HH:MM:SS u DD-MM HH:MM:SS
- Auto-save logova: svaka log linija se append-a u logs.txt u root folderu
- Freeze logovi se zapisuju s prefiksom [FREEZE] da se razlikuju od monitor logova
- Fajl se nikad ne briše automatski — raste s vremenom

v1.0.71 — Fubo: nova platforma + post_refresh_key mehanizam
-----------------------------------------------------------------------
- Fubo: dodana platforma, URL https://www.fubo.tv/
- Fubo: Chrome browser, MD browser size, refresh_first, load_wait 10s
- Fubo: post_refresh_key = " " (space) — šalje space nakon refresha da pokrene video
- Freeze Detection: ručno po potrebi (nije ON po defaultu)
- Novi mehanizam post_refresh_key: šalje key press nakon refresha i load_wait,
  neovisno o click targetima — korisno za platforme koje trebaju keypress za autoplay

v1.0.70 — DStv: prebačen na video_detect mode (kao DAZN)
-----------------------------------------------------------------------
- DStv: više ne klikče button nego refresha i čeka da video krene (kao DAZN)
- DStv: load_wait 60s, freeze_recovery refresh_only
- DStv: stari click selector sačuvan u komentaru u kodu — lako vratiti ako
  DStv opet zatreba klik (PlayerControls_buttonpause)

v1.0.69 — Stan: force_js_click + FanCode: video detect popravci
-----------------------------------------------------------------------
- Novi mehanizam force_js_click: preskače Selenium clickability provjeru i
  koristi direktni JS click (arguments[0].click()) — radi čak i kad element
  ima width:0/height:0 (skriven u DOMu ali prisutan)
- Stan: force_js_click enabled — Watch Live button je u DOMu ali skriven
  dok live nije aktivan; JS click ga uspješno klikne
- Stan: selector promijenjen na //span[contains(@class,"play__label") and text()="Watch Live"]
- FanCode: load_wait povećan na 30s
- FanCode: maknut !v.paused uvjet iz video_detect_js
- FanCode: video_detect_key = "m" — unmute čim se detektira video

v1.0.67 — Stan: nova platforma
-----------------------------------------------------------------------
- FanCode: load_wait povećan na 30s (video treba više vremena za učitavanje)
- FanCode: maknut !v.paused uvjet iz video_detect_js — FanCode autoplay je
  muted pa paused state nije pouzdan; dovoljno je currentTime > 0
- Novi mehanizam video_detect_key: kada video_detect uspije, šalje se key press
- FanCode: video_detect_key = "m" — unmute videa čim se detektira da igra

v1.0.67 — Stan: nova platforma
-----------------------------------------------------------------------
- Stan: dodana platforma, URL https://play.stan.com.au/sport
- Stan: Chrome browser, LG browser size (750×650), refresh_first
- Stan: selector //a[contains(@class,"program__button--play")] (Watch Live button)

v1.0.66 — SPOTV Now JP: pre-click login flow (finaliziran)
-----------------------------------------------------------------------
- Novi mehanizam: pre_click_targets — klikovi koji se izvršavaju PRIJE glavnog
  click cyclea, bez Ctrl i bez tab switchinga (čisti plain click)
- SPOTV Now JP: dodani pre-click targeti za auto-login flow:
    1. //button[contains(@class,"login-btn")] — otvori login formu
    2. //button[contains(@class,"default") and contains(.,"ログイン")] — potvrdi login
  Ako element nije prisutan (korisnik je već logiran), pre-click se preskace (2s timeout)
- pre_click_wait: 5s cekanja NAKON svakog pre-klika (ukljucujuci zadnji) da stranica reagira
- Navigate na https://spotvnow.jp/schedule/0 samo ako je login flow bio triggered
  (ako je korisnik vec logiran, navigate se preskace — ostaje na trenutnom URL-u)

v1.0.64 — Tencent: nova platforma
-----------------------------------------------------------------------
- Tencent: dodana platforma, URL https://sports.qq.com/kbsweb/index.htm
- Tencent: Chrome browser, SM browser size, Freeze Detection OFF (detalji TBD)

v1.0.63 — FanCode: refresh_first + video_detect + freeze detection
-----------------------------------------------------------------------
- FanCode: refresh_first enabled (refreshes page before clicking)
- FanCode: video_detect enabled — waits for video to start before stopping monitoring
- FanCode: freeze_recovery set to refresh_only (same as DAZN)
- FanCode: Freeze Detection ON by default

v1.0.62 — Freeze Detection: fix video selector (always picks active video)
-----------------------------------------------------------------------
- Freeze Detection: querySelector('video') replaced with smarter selector —
  now picks the first non-paused/active video element instead of always the
  first one (which on Disney+ BR/AR is a placeholder with currentTime=0)
- This fixes baseline=0.0 / false freeze stops on platforms with multiple
  video elements (Disney+ BR, Disney+ AR confirmed affected)

v1.0.61 — Disney+ AR + Freeze Detection manual start delay + misc fixes
-----------------------------------------------------------------------
- Disney+ AR: added "Watch Live" selector (live-modal-watch-live-action-button)
- Disney+ AR: fixed click_delay default (2s), MD browser size, Edge browser, Freeze Detection ON
- Disney+ AR: URL updated to https://www.disneyplus.com/en-gb/home
- Disney+, Disney+ SE, Disney+ DK: added "Watch Live" selector (live-modal-watch-live-action-button)
- Disney+ BR: new platform added, Edge browser, MD browser size, URL https://www.disneyplus.com/en-gb/home
- Freeze Detection manual start: added 30s delay (same as automatic) so video has time to load
  before first currentTime sample — fixes false baseline=0.0 when started manually
- Edge browser: --disable-gpu flag now applied to Edge as well (was Chrome only)
- install.bat: added PowerShell fallback if curl fails to download Python installer


v1.0.60 — Freeze Detection: zero-baseline protection + max retry limit
-----------------------------------------------------------------------
- Skip-if-zero-on-first-sample: if baseline currentTime=0.0 (video not started yet),
  freeze detection skips that cycle and retries — up to 3 times before giving up
- Max retry limit: if freeze recovery runs 3 consecutive times and currentTime stays 0.0
  (video unresponsive), freeze detection stops automatically with a warning
- Prevents infinite refresh loops when video player fails to load or currentTime is unavailable
- Both counters reset to 0 whenever a healthy currentTime (>0.0) is detected


v1.0.59 — FanCode platform added
----------------------------------
- Added FanCode platform (fancode.com)
- Default browser: Edge
- Default browser size: LG (750×650)
- Freeze Detection disabled by default (click selectors TBD after first live test)


v1.0.58 — Freeze Detection threshold tuned
-------------------------------------------
- Freeze threshold changed from 80% of CHECK_INTERVAL (24s) to a fixed 15s
- Video must advance at least 15s per 30s check interval to be considered healthy
- Reduces false positives during heavy buffering (buffering pause of ~15s will still pass)
- Still catches real freezes where currentTime stops completely


v1.0.57 — DStv platform + indefinite Freeze Detection
-------------------------------------------------------
- Added DStv platform (dstv.stream/#/livetv/sport, Edge default)
- No click monitoring — Freeze Detection only
- Freeze Detection enabled by default for DStv
- End date/time cleared automatically on DStv select (no end time)
- Freeze Detection now supports indefinite mode: if End date/time fields are empty, runs until manually stopped
- Log shows "runs until ∞ (no end time)" when no end time is set
- End time check skipped in freeze loop when running indefinitely


v1.0.56 — Disable GPU + manual Freeze Detection start
-------------------------------------------------------
- Added --disable-gpu flag for Chrome (not Edge)
- Disables hardware graphics acceleration in Chrome
- Fixes black screen on some PC configurations
- Added "▶ Start Freeze Detection" button in Freeze tab
- Starts Freeze Detection immediately without prior monitoring
- End time: uses entered End date/time, or now+4h if empty
- Start button disables while freeze is running, re-enables when stopped


v1.0.55 — Freeze Detection defaults
-------------------------------------
- Freeze Detection enabled by default for all Prime Video platforms (USA, IT, BR, UK, DE, ES, JP, MX, FR)
- DAZN DE and ES remain unchanged (already enabled by default)


v1.0.1 — Initial release
--------------------------
- Basic tkinter GUI with Setup and Monitor tabs
- Selenium 4 with Chrome support
- Persistent Chrome profile via --user-data-dir
- XPath, CSS Selector, ID, Class Name selector types
- Click targets (one per line)
- Start/stop monitoring with scheduled start time
- Timestamped log with colour-coded entries
- Prime Video as first supported platform


v1.0.2 — Prime Video BR + multi-region base
--------------------------------------------
- Added Prime Video BR (keyword: Assista)
- Added Prime Video IT (keyword: Guarda)
- Added Prime Video USA (amazon.com/gp/video/sports)
- Added Prime Video UK (amazon.co.uk)
- Added Prime Video DE (amazon.de)
- Added Prime Video ES


v1.0.3 — TOD + Disney+
------------------------
- Added TOD platform (selector: ID, target: watch_live_click)
- Added Disney+ platform (data-testid="playback-action-button")
- Refresh before click: disabled by default for TOD, enabled for Prime Video and Disney+


v1.0.4 — Edge browser support
-------------------------------
- Added Microsoft Edge as browser option alongside Chrome
- Edge binary auto-detected at known Windows paths
- Separate edge_profile folder for persistent Edge sessions
- Fixed webdriver-manager conflict with Edge — now uses Selenium Manager directly


v1.0.5 — Auto-update system
-----------------------------
- App checks GitHub on every startup for new version
- Fetches version.txt from GitHub raw URL
- Prompts user with Yes/No dialog if update available
- Downloads new ott_autoclicker.py and restarts via os.execv
- SSL certificate verification bypassed (ssl.CERT_NONE) for compatibility
- UPDATE_VERSION_URL and UPDATE_SCRIPT_URL constants added


v1.0.6 — UI improvements + tooltips
--------------------------------------
- Added Click delay (ms) spinbox with tooltip
- Added Page-load wait (s) spinbox with tooltip
- Added Refresh every (s) spinbox
- Added Refresh BEFORE clicking checkbox with tooltip
- Info icons (ⓘ) with dark tooltip popups for all settings
- Browser + Platform moved to same row to save space
- URL field spans full width
- Test Target button added next to Close Browser
- Click targets textarea height reduced to 3 lines


v1.0.7 — Monitoring fixes
---------------------------
- Fixed: refresh was not actually working (was using send_keys Cmd+R, now uses driver.refresh())
- Fixed: monitoring continued after successful click — now stops immediately when ok > 0
- End time field: leave empty to run indefinitely
- Removed seconds display from time fields


v1.0.8 — Windows deployment
-----------------------------
- Added install.bat: checks for Python, downloads installer via curl if missing, installs selenium
- Added run.vbs: launches app silently via WScript.Shell without terminal window
- Fixed: pythonw not on PATH issue in VBS — switched to plain python
- Platform default changed to empty (no platform selected on startup)
- Window geometry: 600x350, positioned top-right corner of screen


v1.0.9 — Prime Video JP + window polish
-----------------------------------------
- Added Prime Video JP (amazon.co.jp, keyword: Watch)
- Window geometry persistence: saves/restores position via prefs.json
- Window default position: top-right corner (screenwidth - 630, y=40)


v1.0.10 — Version bump
------------------------
- Version bump for Prime Video JP release
- version.txt updated for auto-update distribution


v1.0.11 — Paramount+ + Event keyword + Edge defaults
------------------------------------------------------
- Added Paramount+ platform (paramountplus.com, Edge default)
- Added Event keyword field: filters which event card to click by name (e.g. "Vissel Kobe")
- Event keyword only visible when Paramount+ is selected (hidden for all other platforms)
- Event keyword live-updates XPath in targets field as you type
- Base targets stored per platform; keyword injected at runtime into XPath predicate
- TOD and Paramount+ now default to Edge browser on platform select
- Removed Apple TV+, Max, Netflix from platform list


v1.0.12 — Event keyword polish
--------------------------------
- Event keyword field hidden/shown dynamically on platform change
- Switching away from Paramount+ clears and hides keyword field
- XPath in targets field updates in real time as keyword is typed
- _effective_targets() simplified: reads directly from targets_text (already contains keyword)


v1.0.13 — NBA Docomo + Scroll after click + Coupang Play
----------------------------------------------------------
- Added NBA Docomo platform (nba.docomo.ne.jp/schedule, Edge default)
  - Page-load wait: 10 seconds default
  - Scroll after click: 290px (uses document.body.scrollBy, not window.scrollBy)
  - Click delay: 2000ms
- Added Coupang Play platform (coupangplay.com, Chrome default)
  - Selector: //*[@data-cy="playCtaButtonText"]
- Added Scroll after click (px) field to Setup tab
- Added load_wait per platform rule (page-load wait loaded from PLATFORM_RULES)
- scroll_after and load_wait applied automatically on platform select


v1.0.14 — Inspector tab + launcher exe
----------------------------------------
- Added Inspector tab (third tab, after Monitor)
  - Injects JS click listener, captures tag/classes/href/text/data-* attributes
  - Walks up DOM tree to find meaningful parent element
  - Displays captures in scrollable log with color-coded keys/values
  - Auto re-injects after page refresh or navigation
  - Clear button + Save to file (captures.json)
- Added AutoClicker.exe via PyInstaller (launcher only, never changes)
- Added build_launcher.bat, launcher.py, favicon.ico
- Added Disney+ SE (Edge default — Sweden account)
- Window height increased to 600x400


v1.0.15 — Rollback base + Disney+ DK
---------------------------------------
- Rolled back to stable base (v1.0.14 backup from GitHub)
- Renamed Disney+ Edge to Disney+ SE
- Added Disney+ DK (Denmark account, Edge default, same selectors)


v1.0.16 — Disney+ AR + selector fix
--------------------------------------
- Added Disney+ AR (Argentina account, Chrome default)
- Fixed Disney+ selector: removed contains(.,"CONTINUE") text filter
  - Now uses //*[@data-testid="playback-action-button"] only
  - Applied to all Disney+ variants (Disney+, SE, DK, AR)


v1.0.17 — Use system profile option
--------------------------------------
- Added "Use system profile" checkbox in Setup tab (next to Browser dropdown)
- When checked: Selenium uses the real Chrome/Edge User Data profile (already logged in)
- When unchecked (default): uses browser_profiles/ folder as before
- Warning: close regular browser before using system profile in Selenium


v1.0.18 — Platform dropdown fixes + Amazon Prime US rename
-----------------------------------------------------------
- Renamed "Prime Video" to "Amazon Prime US"
- Fixed Platform combobox alignment
- Browser dropdown made slightly wider (width 14)
- Platform combobox now allows typing/filtering (no longer strictly readonly)
- Disney+ AR: default page-load wait set to 10s


v1.0.19 — Prime Video MX
--------------------------
- Added Prime Video MX (primevideo.com, Edge default)
  - Selector: contains(.,"Ver")


v1.0.20 — Disney+ modal selector + load_wait bug fix
------------------------------------------------------
- All Disney+ variants (Disney+, SE, DK, AR): added modal-action-button
  as first target, playback-action-button as fallback
  - Handles popup ("Watch Live / Watch from Beginning") that appears
    when not entering live immediately
- Fixed bug: switching away from Disney+ AR (load_wait 10s) to another
  platform no longer keeps the 10s page-load wait — resets to 5s default


v1.0.21 — JS click fallback + NBA Docomo space-key playback
------------------------------------------------------------
- JS click fallback: when Selenium can't click due to overlapping element
  (ElementClickInterceptedException), automatically retries via JavaScript
  click which bypasses visual interception — works for all platforms
- NBA Docomo playback sequence overhauled:
  - Selector changed to //video-js[contains(@class,"video-js")] (VideoJS container)
  - Sequence: refresh → load wait → scroll 290px → click video-js (focus) → space key
  - Scroll now happens BEFORE click when key_press is configured
  - Space key triggers live playback after video player has focus
- key_press field added to platform rules


v1.0.22 — Use system profile: kill browsers dialog
---------------------------------------------------
- "Use system profile" checkbox now shows a confirmation dialog when checked
- Dialog offers: Kill Chrome / Kill Edge / Both / Cancel
- Selecting Kill Chrome/Edge/Both force-closes running browser + driver processes
  before Selenium tries to attach (prevents "profile locked" session errors)
- Selecting Cancel reverts the checkbox to unchecked
- Dialog centered relative to the main app window
- Works on Windows (taskkill) and macOS (pkill)
- Spinbox scroll px display fix: force-refreshes value on platform change
- NBA Docomo default page-load wait reduced from 10s to 5s


v1.0.23 — GPU compositing flags
---------------------------------
- Added GPU flags to reduce black screen on region capture:
  --disable-gpu-compositing, --disable-accelerated-2d-canvas,
  --disable-accelerated-video-decode (applied to both Chrome and Edge)


v1.0.24 — Prime Video FR + auto-update fix
------------------------------------------
- Added Prime Video FR (primevideo.com, Chrome default)
  - Selectors without keyword filter: data-automation-id + data-testid only
  - No contains() text filter — ID selectors are sufficient
- Fixed auto-updater bug: version comparison was using != instead of >
  - App would offer to "update" to older GitHub version if local version was newer
  - Now uses proper tuple comparison so only genuinely newer versions trigger update


v1.0.25 — Default browser fix per platform
-------------------------------------------
- Fixed: switching platforms now always resets browser to correct default
  - Edge platforms (TOD, Paramount+, NBA Docomo, Disney+ SE/DK, Prime Video MX) → Edge
  - All other platforms → Chrome
  - Previously, switching from an Edge platform to a Chrome platform kept Edge selected


v1.0.26 — Browser window position memory
-----------------------------------------
- Chrome and Edge browser window position/size now saved separately in prefs.json
- On close: saves current position and size per browser (chrome_browser / edge_browser)
- On open: restores last saved position and size for that browser
- Default size remains 550x450 if no saved position exists yet
- Refactored prefs.json handling: _load_prefs() and _save_prefs() helpers


v1.0.27 — Prime Video selectors unified (no keyword filter)
-----------------------------------------------------------
- Removed contains() keyword filter from all Amazon Prime / Prime Video platforms:
  Amazon Prime US, Prime Video USA, IT, BR, UK, DE, ES, JP, MX, FR
- All now use only: //*[@data-automation-id="circular-playbutton"]
  and //*[@data-testid="play"] — ID selectors sufficient for all regions
- Default browser per platform remains unchanged


v1.0.28 — IP address display
------------------------------
- Added IP address label at top of Setup tab
- Shows current public IP and country on startup: e.g. "IP: 185.x.x.x  (Germany)"
- Useful for verifying VPN is active before starting monitoring
- Refresh button (↺) to re-fetch IP manually at any time
- Uses ip-api.com (HTTP, no SSL issues)


v1.0.29 — Edge driver + launcher fixes
----------------------------------------
- Edge binary detection: added Windows registry lookup (most reliable) + fallback
  to %LOCALAPPDATA%\Microsoft\Edge\Application\msedge.exe as third path
- msedgedriver lookup: searches app folder, PATH, and Selenium Manager cache
  (~/.cache/selenium / %LOCALAPPDATA%\selenium) before falling back to Selenium
  Manager network download — fixes Edge not opening when VPN blocks the download
- System profile: auto-detects the last-used profile directory from Local State
  JSON (e.g. "Profile 1" instead of hardcoded "Default") — fixes Selenium opening
  a fresh empty profile instead of the real signed-in profile
- GPU flags (--disable-gpu etc.) now applied to Chrome only, not Edge
- run.vbs: finds Python via Windows registry (HKLM + HKCU, versions 3.6–3.13)
  instead of relying on PATH — fixes app not starting after PC restart
- Auto-update now also updates run.vbs alongside ott_autoclicker.py
- Coupang Play default browser changed from Chrome to Edge


v1.0.30 — Compact Mode
------------------------
- Added Compact Mode button in status bar (bottom right)
- Clicking "Compact Mode" hides the notebook tabs and shrinks the window to a
  minimal bar showing only status + last log line
- Status bar (with "Expand" button) always remains visible in compact mode
- Clicking "Expand" restores the full window to its original size and position


v1.0.31 — SPOTV Now JP
------------------------
- Added SPOTV Now JP platform (spotvnow.jp/schedule/0, Chrome default)
- Event keyword field (same as Paramount+): type team name to target that
  specific match row — e.g. "Cleveland Guardians"
- Page-load wait: 8s (SPA needs time to render the schedule)
- Selector targets view-box live class (live matches only, not reservations)
- Post-click popup handling: after clicking the live button, waits 4s and
  automatically clicks the "リアルタイムで視聴" (watch live) confirmation button
  if it appears — skips silently if popup is not present


v1.0.32 — SPOTV Now JP Ctrl+click (open in tab)
-------------------------------------------------
- SPOTV Now JP: click changed to Ctrl+click so live page opens in a new
  browser tab (same window) instead of a new popup window
- New platform rule field: ctrl_click — when True, uses ActionChains
  key_down(CTRL) → click → key_up(CTRL) instead of regular Selenium click
- New platform rule field: post_switch_wait — extra wait (seconds) after
  switching to the new tab, before clicking the post-click popup target
- SPOTV Now JP timing: 3s wait → switch to new tab → 3s wait → click popup
- post_switch_wait added to _loop() between tab switch and post_click_targets
- ctrl_click and post_switch_wait default to False/0 — no other platforms affected


v1.0.33 — Auto-recover from closed live tab
--------------------------------------------
- Fixed: if the live tab was closed after a successful SPOTV click, the next
  monitoring cycle would crash with "no such window: target window already closed"
- _loop() now checks at the start of each cycle if the driver is still on a
  valid window — if not, automatically switches back to window_handles[0]
  (the schedule tab) and logs "switched back to main tab"
- Works for any platform that opens a second tab/window during monitoring


v1.0.34 — Peacock
------------------
- Added Peacock platform (peacocktv.com, Edge default)
- Selector: //*[@data-testid="watch-button"] (Watch Now button)
- URL: set manually to the specific event page before monitoring
- refresh_first: True, click_delay: 2000ms, load_wait: 10s


v1.0.35 — DAZN video detect
-----------------------------
- Added DAZN DE platform (dazn.com/en-DE/home, Chrome default)
- New "video_detect" mode: no click target needed — refreshes page and checks
  via JavaScript whether a video element has started playing
- JS condition checked each cycle: video not paused, no error, currentTime > 0,
  readyState >= 3 (HAVE_FUTURE_DATA) — monitoring stops when all are true
- JS hardcoded in platform rule (video_detect_js field) — not exposed in UI
- start_monitoring() skips "no targets" warning when video_detect mode is active
- Default: refresh_first=True, load_wait=60s, refresh every 60s (120s total cycle)


v1.0.36 — SPOTV Now JP default browser size
--------------------------------------------
- SPOTV Now JP: default browser window size set to 650×550
- Platform rule browser_size field: overrides saved prefs when opening browser
- All other platforms remain at 550×450 default


v1.0.37 — EdgeDriver auto version check + DAZN ES
---------------------------------------------------
- EdgeDriver cache version now checked against installed Edge major version
- If versions don't match (e.g. cached v146, Edge updated to v148): skips
  cached driver and lets Selenium Manager download the correct version
- Version extracted from cache path or --version output; Edge version read
  from Windows registry (HKLM\SOFTWARE\Microsoft\Edge\BLBeacon)
- No more manual cache deletion needed after Edge auto-updates
- Added DAZN ES platform (dazn.com/en-ES/home, Edge default)
  - Same video_detect mode as DAZN DE (load_wait 60s, refresh_first)


v1.0.54 — EdgeDriver auto-cache
---------------------------------
- After Selenium Manager downloads a new EdgeDriver, the app copies it to the local app folder
- Next browser open finds the local driver immediately — no 40s wait
- Copy only happens if driver version matches installed Edge version
- Copy failure is non-fatal — silently logs WARN and continues normally


v1.0.53 — Simple/Advanced Mode + UI polish
-------------------------------------------
- Dodan gumb "Advanced" u statusnu traku (lijevo od "Compact")
- Po defaultu app startuje u Simple modu — skriveni: Browser size, Selector,
  Click delay, Load wait, Refresh, Scroll, Freeze Detection, Targets, separatori
- Sakriveni gumbi u simple modu: Reposition, Kill Browser, Test Target
- Sakriveni tabovi u simple modu: Inspector, Channels
- Klik na "Advanced" prikazuje sve, gumb mijenja tekst u "Simple"
- Compact Mode preimenovan u "Compact", Advanced Mode u "Advanced"
- Compact prozor povećan: 400×60 → 500×80
- Advanced gumb se skriva kad je Compact mode aktivan
- Mute premješten u prvi red gumba (uz Open/Navigate/Close)
- Reposition premješten u drugi red (uz Kill Browser, Test Target)
- Platform dropdown: height=20 (sve platforme vidljive bez scrollanja)


v1.0.52 — Browser size dropdown + Test Target premješten
----------------------------------------------------------
- Dodan "Browser size" dropdown ispod URL polja: SM (550×450), MD (650×550), LG (750×650)
- Promjena dropdowna live-resizea browser ako je otvoren
- SPOTV Now JP po defaultu dobiva MD, sve ostale platforme SM
- Test Target premješten u drugi red gumba, iza Kill Browser


v1.0.51 — Reposition browser
------------------------------
- Dodan gumb "Reposition" pored Close Browser
- Vraća browser na zadnju spremljenu poziciju i veličinu iz configa
- Ako nema spremljene pozicije, prikazuje poruku da korisnik zatvori browser normalno


v1.0.50 — UI cleanup
---------------------
- Browser selector: dropdown → radio buttons (Chrome / Edge jedan pored drugog)
- Logika defaultnog browsera po platformi ostaje ista
- Navigate gumb: maknuta strelica → samo "Navigate"
- Kill Browser: maknuta 💀 ikona → samo "Kill Browser"
- Inspector tab: maknut "Save to file" gumb
- Custom URL: maknuto iz Platform dropdowna
- Separator dodan između Selector i Click delay sekcije u Setup tabu
- Date fields: dodan + gumb za +1 dan pored Start/End date polja (Monitor i Freeze tab)


v1.0.49 — Removed "Use system profile" feature
------------------------------------------------
- Removed "Use system profile" checkbox from Setup tab
- Removed _on_sys_profile_toggle() and all related logic
- Browser always uses autoclicker profile (browser_profiles/ folder)
- Kill Browser button remains (unaffected)


v1.0.48 — Freeze Detection improvements
----------------------------------------
- Added Start date/time and End date/time fields in Freeze tab
- Auto-populated when Freeze Detection activates:
  Start = moment monitoring stops (click succeeded)
  End = Start + 4 hours
- User can manually edit End time before Freeze Detection starts
- Check interval reduced from 60s to 30s (faster freeze detection)
- Fixed freeze detection logic: delta must be ≥80% of CHECK_INTERVAL (24s out of 30s)
  Previously any positive delta was treated as OK — now small advances are correctly
  detected as freezes (e.g. +6.5s in 30s → freeze)
- Freeze Detection starts 60s after monitoring stops (initial delay preserved)


v1.0.47 — Click delay in seconds
----------------------------------
- Click delay changed from milliseconds to seconds
- Default: 1s (was 1000ms), platform defaults: 2s (was 2000ms)
- Spinbox range: 0–300s
- Internal: time.sleep(delay_s) instead of time.sleep(delay_ms / 1000)


v1.0.46 — Freeze Detection UX improvements
-------------------------------------------
- Freeze Detection checkbox moved to same row as "Refresh BEFORE clicking"
- DAZN DE and DAZN ES: Freeze Detection enabled by default on platform select
- First check now happens immediately when Freeze Detection starts (no 60s initial wait)
- Status bar shows "Freeze Detection active" while running


v1.0.45 — Freeze Detection: smart recovery per platform
--------------------------------------------------------
- DAZN DE / DAZN ES: freeze recovery = "refresh_only" — page refresh is enough,
  video resumes automatically after reload
- All other platforms (Amazon, Peacock, etc.): freeze recovery = "remonitor" —
  after refresh, automatically restarts click monitoring so the Watch Live /
  Resume popup gets clicked again
- Freeze end time preserved across remonitor cycles (stays at original start + 4h)
- New platform rule field: freeze_recovery ("refresh_only" | "remonitor")


v1.0.44 — Freeze Detection (experimental)
------------------------------------------
- Added "Freeze Detection" checkbox in Setup tab
- After a successful click monitoring stops, Freeze Detection starts 60s later
- Checks video.currentTime every 60s — if it hasn't advanced, treats it as a freeze
- On freeze: refreshes the page, waits 60s, then resumes checking
- Runs for 4 hours from the monitoring start time (auto end time)
- New "Freeze" tab (between Monitor and Inspector) with dedicated log and Stop button
- Auto-stops if browser is closed
- Stop button available in Freeze tab at any time


v1.0.43 — Kill Browser button
------------------------------
- Added 💀 Kill Browser button in Setup tab (second row of buttons)
- Force-kills all Chrome or Edge processes (browser + driver) for the selected browser
- Useful when browser hangs or won't close normally
- Buttons reorganized into two rows: Open/Navigate/Close/Test on row 1, Mute/Kill on row 2


v1.0.42 — Mute/Unmute browser
------------------------------
- Added 🔇 Mute / 🔊 Unmute toggle button in Setup tab (next to Test Target)
- Mutes or unmutes all video and audio elements in the browser via JavaScript
- Button label toggles between Mute and Unmute to show current state
- Works with any platform — browser must be open


v1.0.41 — Channels tab
-----------------------
- Added Channels tab (4th tab) showing all monitored channels from GitHub
- Fetches channels.csv from GitHub on first open (lazy load) — no startup delay
- Color-coded by browser: Chrome (yellow), Edge (blue), Both (green), ? (grey)
- Columns: Country, Channel, ID, PC, Status
- Refresh button to manually reload the list at any time
- channels.csv hosted on GitHub alongside the script


v1.0.40 — Platform list cleanup
---------------------------------
- Removed "Amazon Prime US" platform (duplicate of Prime Video USA)
- Prime Video USA remains (amazon.com/gp/video/sports)


v1.0.39 — No-sandbox mode for server/VM environments
------------------------------------------------------
- Added --no-sandbox and --disable-dev-shm-usage flags to both Chrome and Edge
- Fixes "Chrome instance exited" crash on Windows Server, VMs, and restricted
  corporate configurations where the browser sandbox cannot initialize


v1.0.38 — Removed GPU flags from Chrome
-----------------------------------------
- Removed --disable-gpu, --disable-gpu-compositing, --disable-accelerated-2d-canvas,
  --disable-accelerated-video-decode from Chrome launch options
- These flags caused "Chrome instance exited" crash on some PC configurations
- They were originally added in v1.0.23 for World2Capture black screen but
  never solved that issue — removed entirely


─────────────────────────────────────────────────────────────
  PREVIEW / REVERTED — istraživanje black screen problema
─────────────────────────────────────────────────────────────

[PREVIEW] v1.0.34 — GPU flagovi za Chrome (reverted)
------------------------------------------------------
Cilj: popraviti crni ekran u World2Capture region/window capture.
Isprobano redom:
- --disable-gpu, --disable-gpu-compositing, --disable-accelerated-2d-canvas,
  --disable-accelerated-video-decode (od v1.0.23, ostalo aktivno)
- --use-gl=swiftshader (potpuni software renderer, zaobilazi GPU/ANGLE/D3D)
- --use-angle=swiftshader (forsira ANGLE software path)
- --disable-features=VizDisplayCompositor
Zaključak: nijedan flag nije pomogao. Problem nije u GPU flagovima nego u
načinu na koji Selenium pokreće Chrome (--remote-debugging-port CDP konekcija
mijenja DWM kompoziciju prozora). Reverted.


[PREVIEW] v1.0.35 — Chrome subprocess launch (reverted)
---------------------------------------------------------
Cilj: pokrenuti Chrome kao normalan OS subprocess (ne preko Selenium
webdriver.Chrome()) pa da World2Capture može čitati prozor.
Implementirano:
- subprocess.Popen(chrome.exe --remote-debugging-port=9222 ...)
- čeka 3s → Selenium se spaja via debuggerAddress CDP
- Connect button za spajanje na već-otvoreni Chrome
- chrome_proc cleanup u close_browser()
Potvrđeno: --remote-debugging-port sam po sebi NE kvari World2Capture
capture (testom ručnog pokretanja Chrome s flagom capture je radio).
Problem: kompleksnost spajanja, Chrome ignorira flag ako je već pokrenut,
korisnik mora killati sve chrome.exe procese. Reverted, nastavlja se kasnije.
